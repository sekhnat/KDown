# Tasks

## 1. Repair observed platform failures

- [ ] 1.1 Replace the Unix-only `read_exact_at` test call in `crates/engine/src/io/positional.rs` with portable post-write read-back while retaining production positional writes; verify the positional unit tests compile and pass on Windows and Unix.
- [ ] 1.2 Cover disjoint positional writes and large absolute offsets on Windows with a narrow real-file regression test (sparse/limited read-back where feasible); verify `cargo test --workspace --all-targets` on a Windows runner without disabling coverage.
- [ ] 1.3 Reproduce the failing `h1_adaptive_reverts_when_marginal_gain_disappears` macOS scenario, record desired-worker history and measured/fixture penalty behavior, and distinguish fixture/observation error from policy error; verify repeated targeted macOS runs produce an explained outcome.
- [ ] 1.4 Fix the demonstrated macOS fixture/observation or adaptive-policy defect and add deterministic reversion regression coverage while preserving final byte equality; verify targeted macOS integration tests and deterministic `control::adaptive` unit tests pass repeatedly.

## 2. Enforce truthful CI and MSRV

- [x] 2.1 Check Rust 1.85 against the full intended workspace dependency graph, including all-target code, and document any incompatible transitive crate or source feature; verify an exact-1.85 `cargo check --workspace --all-targets` result from an installed CI toolchain. Verified locally: exact rustc 1.85.0 compiled the locked workspace (149 crates, incl. all targets) after pinning `rcgen 0.14.0` + `time 0.3.41` (dev-only cert generation); the only remaining >1.85 declaration is WASI-gated and never compiled on supported targets.
- [x] 2.2 Make dependency resolution reproducible in CI (prefer a reviewed tracked workspace lockfile and `--locked`, with library-consumer caveat); if 1.85 is unsupported without compromising needed dependencies, find the lowest viable compiler and update workspace metadata accordingly; verify both chosen-MSRV and pinned-1.98.1 workspace/all-target checks succeed on the same intended graph. `Cargo.lock` is now tracked (`--locked` verified on both rustc 1.85.0 and 1.98.1); workspace `rust-version` stays 1.85.
- [ ] 2.3 Add a dedicated exact-MSRV job to `.github/workflows/ci.yml` without replacing the Ubuntu/macOS/Windows pinned-toolchain build/test/clippy matrix, docs, or benchmark smoke checks; add/keep formatting and useful named step diagnostics; verify workflow results report each OS job and MSRV independently.

## 3. Prevent diagnostic credential exposure

- [x] 3.1 Implement a shared formatting policy that shows custom header names but never values in `DownloadRequest::Debug` and `RequestSpec::Debug`, retaining authorization/provider/proxy redaction; verify unit tests reject exact unique sentinel values for Authorization, Proxy-Authorization, Cookie, X-Api-Key, and an arbitrary name while accepting visible names. Implemented `redact::RedactedHeaders` (+ `REDACTED_VALUE`) and applied it in both `Debug` impls; sentinel tests pass in `tests/request_redaction_tests.rs` and `redact.rs`.
- [x] 3.2 Audit nested probe/transfer request debug, proxy config/connector debug, and scripted expectation, mismatch, call-record, and logging surfaces; apply the same safe formatting at diagnostic boundaries without changing request matching; verify lower-level debug/error and captured-log tests cannot contain the sentinels. Audited: `RequestSpec` now has a manual `Debug` (covers nested probe/transfer wrappers), `CallRecord::Debug` and the three scripted expectation descriptions redact values, `header_mismatches` no longer prints expected values, and `ProxyConfig`/`EngineConfig` debug redacts proxy URL userinfo (connector debug prints no credentials); no log path prints request headers (connect debug already used `Redactor`). Request matching is unchanged (raw values still compared); suite green.

## 4. Correct runtime-control events

- [x] 4.1 Add rustdoc and `Event::ConcurrencyChanged { workers: u64 }`; apply/clamp manual segmented concurrency before emitting one corresponding event and never emit `RateLimitChanged` for that operation; verify subscribed event-stream tests for applied count, clamping, no segmented job, and absence of false rate events. `SegmentedJob::set_desired_workers` now returns the applied clamped count; update+emission pairs are serialized by a handle-local control mutex; all four scenarios pass in `runtime_control_tests`.
- [x] 4.2 Make `set_rate_limit` publish its existing rate event only after the stable bucket update, with `None` for unlimited and no concurrency event; verify pre-start/live rate updates and subscribed event ordering with positive and zero limits. Verified by `rate_limit_updates_emit_rate_limit_changed_only` (pre-start `Some(1 MiB)` then live `None`, in order, no concurrency event).

## 5. Unify hierarchical rate-limit arbitration

- [x] 5.1 Correct `RateLimiter::acquire` to select the maximum active wait without double debit; verify deterministic helper and bucket tests for global>job, job>global, equality, each single enabled level, and both unlimited. Added `dominant_wait` (shared combiner) with exact unit tests plus zero-burst bucket tests covering all six cases and one-debit-per-bucket; all pass.
- [x] 5.2 Replace the duplicated sequential and segmented arbitration in `job/controller.rs` and `job/segmented.rs` with the shared semantics, preserving stable bucket identity, fast unlimited paths, and cancellable sleeps; verify existing rate-limit integration tests plus targeted cancellation-while-waiting and per-bucket accounting tests pass. Both hot paths now call `dominant_wait`; existing rate-limit integration tests pass and a sequential cancellation-during-wait regression was added (`cancel_stops_sequential_rate_wait_promptly`).

## 6. Rename controller and preserve migration

- [x] 6.1 Rename the single implementation and public re-exports to `DownloadController` in the controller module and crate root; retain a deprecated `SingleStreamController` alias with migration message if it compiles on the supported compiler; verify a root-import compile test and a locally deprecation-allowed old-module/root alias compatibility test resolve to the same implementation. Alias compiles and `tests/controller_compat_tests.rs` proves type/method identity (deprecation allowed only in that test); clippy `-D warnings` clean.
- [x] 6.2 Update crate rustdoc, `crates/engine/examples/download.rs`, README snippets, tests/imports, and controller/origin wording to the canonical name; if aliasing is materially impossible, record the breaking rationale and migration path in design/release notes; verify examples and doc tests compile with the canonical API and clippy has no unintended deprecation warnings. All renamed (historical bench reports left as frozen evidence); `cargo doc` with `-D warnings` and clippy pass with no deprecation warnings.

## 7. Documentation and end-to-end release gate

- [x] 7.1 Replace task-ID-only and bare spec-section comments in touched code and core job/control orchestration with durable ordering, durability, security, or race rationale; verify a review/search shows no unexplained temporary IDs and code behavior is unchanged by comment-only edits. All `crates/engine/src` task-ID citations removed (searched); descriptive parenthetical content preserved; §/design citations kept only where paired with plain-language rationale; full suite green after the edits.
- [x] 7.2 Clarify in README and `docs/benchmark-profiling.md` that preserved local synthetic baseline records are host-specific, not WAN speedup guarantees; list server range/CDN throttling, latency/RTT, bandwidth, HTTP version, connection caps, storage, CPU, and environment factors; verify CI remains smoke-only and has no absolute workstation throughput threshold. Both documents updated; CI `bench-check` remains `--smoke` with no throughput assertions.
- [x] 7.3 Add an Unreleased changelog entry and align README/rustdoc MSRV, controller migration, redaction, event, rate-limit, and platform notes with verified tests; verify each claimed fixed behavior has appropriate regression evidence and any breaking fallback is conspicuous. Changelog `Unreleased` section added; README states MSRV 1.85 and benchmark interpretation; rustdoc documents both control events and the controller rename; every claimed fix has a named regression test.
- [x] 7.4 Run the repository's formatting check, workspace check/build, full `cargo test --workspace --all-targets` (including integration/property and documentation tests as applicable), `cargo clippy --workspace --all-targets -- -D warnings`, rustdoc with warnings denied, benchmark target compilation and smoke, and exact declared-MSRV CI check; verify all pass without ignored new failures. All local checks pass: `cargo fmt --check` clean, 34 test binaries green (exit 0, no ignored/skipped regressions), 3 doctests pass, clippy `-D warnings` clean, rustdoc `-D warnings` clean, bench compiles and `bench_check.sh --smoke` exits 0, and exact rustc 1.85.0 `check --workspace --all-targets --locked` passes on the final source. Hosted cross-platform CI runs are the remaining gate (task 7.5).
- [ ] 7.5 Validate this change with `openspec validate harden-engine-correctness-security-and-release-readiness --strict`, resolve all findings, and confirm final hosted Linux/macOS/Windows CI plus dedicated MSRV job are green; do not mark release readiness complete until observed results support it.
